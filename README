Python libraries for DELPH-IN

Subdirectories are organized by library category (e.g. tdl, mrs, etc)
or by task (evaluation, etc).

Example usage (load a SimpleMRS structure and re-print it with formatting):

from delphin.mrs import simplemrs
m = simplemrs.load(open('mrsfile.mrs'))
print(simplemrs.encode(m, pretty_print=True))
